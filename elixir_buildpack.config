# Erlang version
erlang_version=19.0

# Elixir version
elixir_version=1.3.0

# Always rebuild from scratch on every deploy?
always_rebuild=true

# Export heroku config vars
config_vars_to_export=(DATABASE_URL POOL_SIZE SECRET_KEY_BASE TMDB_API_KEY)
